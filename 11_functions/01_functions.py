# Every function is declared with def keyword in python , followed by function name according to task ending by :

def average(a,b,c):
    d =(a+b+c)/3
    # print(d)  
    return d     #None will be printed if we do not  use return function
o1 = average(3,4,5)
print(o1)